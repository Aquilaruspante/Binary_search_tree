levelOrder(callback, node=this.root, queue = []) {
    if not callback throw Error;

    queue = [...queue, ...#leveltraverse(node)];

    callback(queue[0]);

    queue.shift();

    levelOrder(callback, node[0], queue);
}

#levelTraverse(node) {
    return [node, node.left, node.right];
}